# Mixing Strings and Variables in className for React JSX

```
<section className="content">
<section className="{highlight-active}">
```

Use template literal.

```
<section className={`${highlight-active} content`}
```